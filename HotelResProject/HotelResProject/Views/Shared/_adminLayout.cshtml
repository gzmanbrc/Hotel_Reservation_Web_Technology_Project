<!--Login olduktan sonraki çıkan dashboard sayfası-->
<!--bu sayfa yerine ana programda direkt ana sayfaya bağlanacak-->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hotel Dashboard</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css">
    <style>
        .dashboard-container {
            max-width: 1200px;
            margin: 50px auto;
            padding: 20px;
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .welcome-text {
            font-size: 24px;
            color: #24416B;
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-top: 30px;
        }

        .dashboard-card {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }

            .dashboard-card h3 {
                color: #24416B;
                margin-bottom: 15px;
            }

            .dashboard-card p {
                color: #666;
                margin-bottom: 10px;
            }

        .logout-btn {
            background-color: #CC8C18;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

            .logout-btn:hover {
                background-color: #b37a15;
            }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1 class="welcome-text">Welcome to Your Dashboard</h1>
            <button class="logout-btn" onclick="logout()">Logout</button>
        </div>

        <div class="dashboard-grid">
            <div class="dashboard-card">
                <h3>My Bookings</h3>
                <p>View and manage your hotel bookings</p>
                <button class="flex1">
                    <span>View Bookings</span>
                    <i class="fas fa-arrow-circle-right"></i>
                </button>
            </div>

            <div class="dashboard-card">
                <h3>Profile Settings</h3>
                <p>Update your personal information</p>
                <button class="flex1">
                    <span>Edit Profile</span>
                    <i class="fas fa-arrow-circle-right"></i>
                </button>
            </div>

            <div class="dashboard-card">
                <h3>Payment History</h3>
                <p>View your payment transactions</p>
                <button class="flex1">
                    <span>View Payments</span>
                    <i class="fas fa-arrow-circle-right"></i>
                </button>
            </div>
        </div>
    </div>
    @RenderBody()

    <script>
        function logout() {
            // Add logout logic here
            window.location.href = 'login.html';
        }
    </script>
</body>
</html> 